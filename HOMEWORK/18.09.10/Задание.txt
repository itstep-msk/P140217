1) В нашей библиотеке(mini-jquery) doc модицифировать метод css чтобы он мог принимать объект (Клюс(CSS свойство): Значение(CSS значение)).
Пример doc.find(".class").css({"background":"green"})

2) В файле coords в произвольном месте создать элемент. При перемещении зеленого элемента если по одной из сторон есть соприкосновение, то один из элементов меняет цвет. 